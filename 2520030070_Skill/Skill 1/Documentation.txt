SKILL 1

TITLE:
Process Abstraction, fork(), exec(), Parent-Child Relationships and System 
Call Tracing

OBJECTIVES:
1. Install and understand a Linux virtual machine.
2. Configure GCC.
3. Set up a Git repository.
4. Create a project structure.
5. Understand shell architecture.
6. Build an initial Makefile.
7. Analyze process abstraction.
8. Execute fork().
9. Understand the exec() family.
10. Analyze parent-child relationships.
11. Inspect the process tree.
12. Practice system call tracing.

PROGRAM:
src/process_demo.c

MAKEFILE:
Makefile

PROCESS CONCEPTS:

fork():
Creates a new child process from the calling parent process.

exec():
Replaces the current process image with a new program.

wait():
Allows the parent process to wait until the child process completes.

getpid():
Returns the process ID of the current process.

getppid():
Returns the process ID of the parent process.

PROGRAM FLOW:

START
  |
  v
Parent Process
  |
  v
fork()
  |
  +------------------+
  |                  |
  v                  v
Parent              Child
  |                  |
  |               getpid()
  |                  |
  |               getppid()
  |                  |
  |                 exec()
  |                  |
  |                  v
  |                  ls
  |                  |
  v                  v
wait() <--------- Child exits
  |
  v
Parent exits
  |
  v
END

SYSTEM CALLS:
fork()
exec()
wait()
getpid()
getppid()

SYSTEM CALL TRACING:
Linux provides the strace utility for tracing system calls generated by a 
process. The tracing activity will be performed inside the Linux virtual 
machine.

CONCLUSION:
This practical demonstrates process creation, process replacement, 
parent-child relationships, process synchronization, process-tree inspection, 
and the role of system calls in operating-system process management.
